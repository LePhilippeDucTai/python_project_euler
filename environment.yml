name: dev
channels:
  - defaults
dependencies:
  - numpy
  - numba
  - pylint
  - pytest
  - black
  - psutil
  - sympy
prefix: /Users/philippe_le/opt/anaconda3/envs/dev

# To save the environment file :
# conda env export --from-history > environment.yml